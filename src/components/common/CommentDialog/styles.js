import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(theme => ({
  root: {
   
  },
  textarea: {
    width: '100%',
    backgroundColor: '#444',
    resize: 'none',
    outline: 'none',
    color: 'white',
    padding: 10,
  },
}))
